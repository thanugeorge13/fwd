import cors from 'cors';

const corsOptions = {
    origin: 'http://localhost:1000', // Replace with your frontend's URL
    methods: ['GET', 'POST', 'PUT', 'DELETE'],
    allowedHeaders: ['Content-Type', 'Authorization'],
};

export default cors(corsOptions);

